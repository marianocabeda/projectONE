/**
 * Variables de entorno - Generado automáticamente
 * ⚠️ NO EDITAR - Este archivo es generado por el sistema de despliegue
 */
(function() {
  'use strict';
  if (window.__ENV_INITIALIZED) return;
  window.__ENV_INITIALIZED = true;

  // Configuración inyectada en build-time
  window.ENV = {
    API_BASE_URL: 'https://oneinternet.online/backend/v1',
    isDevelopment: false,
    isStaging: false,
    isProduction: true
  };


})();
